# Supabase
SUPABASE_URL=
# Service role — bypasses row-level security. Server-side only, never in the
# frontend and never committed.
SUPABASE_SERVICE_ROLE_KEY=

# Google Gemini (generation + embeddings)
GEMINI_API_KEY=

PORT=3001
