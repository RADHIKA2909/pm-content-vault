{
  "name": "pm-content-vault",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "//": "These are the API's dependencies. Vercel installs from the manifest at the repo root when building the functions in /api, and /api/index.js pulls in backend/src — so the backend's dependencies have to be resolvable from here. backend/package.json keeps its own copy for local development.",
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^2.0.0",
    "pdf-parse": "^1.1.1"
  }
}
